<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :eventname %><br>
    <%= f.text_field :eventname %>
  </div>
  <div class="field">
    <%= f.label :eventdesc %><br>
    <%= f.text_area :eventdesc %>
  </div>
  <div class="field">
    <%= f.label :eventstart %><br>
    <%= f.datetime_select :eventstart %>
  </div>
  <div class="field">
    <%= f.label :eventend %><br>
    <%= f.datetime_select :eventend %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
